# Import everything we need
from pylab import *

# Define time constants and number of steps
dt = 0.01
N = 300

# Define empty arrays
t = zeros(N+1)
a = zeros(N+1)
v = zeros(N+1)
x = zeros(N+1)

# Define inital conditions
v[0] = ...
x[0] = ...

# Define the force
def F(x, v, t)
	return ...

# Euler method loop
for i in range(N):
	t[i+1] = t[i] + dt
	a[i] = F(...)/m
	v[i+1] = ...
	x[i+1] = ...

# Plot results
plot(...)
...
	
